const getBookings = (req, res) => {
  res.status(501).json({ message: "Get bookings endpoint not implemented yet." });
};

const createBooking = (req, res) => {
  res.status(501).json({ message: "Create booking endpoint not implemented yet." });
};

module.exports = {
  getBookings,
  createBooking,
};
